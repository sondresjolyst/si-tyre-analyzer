[mypy]
ignore_missing_imports = True

# PySide6 ships type stubs whose Qt enums (Qt.AlignCenter, QMessageBox.Yes, …)
# differ from the runtime aliases the code uses; treat it as untyped.
[mypy-PySide6.*]
follow_imports = skip
follow_imports_for_stubs = True
