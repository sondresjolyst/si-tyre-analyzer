name: 🧹 Lint

on:
  pull_request:
    branches:
      - main
    types:
      - opened
      - edited
      - synchronize
      - reopened
    paths-ignore:
      - "CHANGELOG.md"
      - ".release-please-manifest.json"
      - "platformio.ini"

permissions: {}

jobs:
  lint-codebase:
    name: Lint codebase
    uses: equinor/ops-actions/.github/workflows/super-linter.yml@7e3b7a3ebdfe8bac700e5714e9bfd6b5b33ed6a9
    permissions:
      contents: read
      statuses: write
    with:
      filter_regex_exclude: (.*CHANGELOG\.md$|docs/workflows/.*|lib/.*|tools/.*_preview\.html$)
